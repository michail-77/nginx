apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-nginx.fullname" . }}
  labels:
    {{- include "image-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "image-nginx.selectorLabels" . | nindent 4 }}
